# Server Configuration
PORT=3004
NODE_ENV=development

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=eatr_delivery
DB_USER=postgres
DB_PASSWORD=postgres

# Kafka Configuration
KAFKA_BROKERS=localhost:9092
KAFKA_CLIENT_ID=delivery-service
KAFKA_GROUP_ID=delivery-service-group

# JWT Configuration
JWT_SECRET=your_jwt_secret_key

# Redis Configuration (for caching driver locations)
REDIS_HOST=localhost
REDIS_PORT=6379

# Google Maps Configuration (for distance calculations)
GOOGLE_MAPS_API_KEY=your_google_maps_api_key

# Driver Assignment Configuration
INITIAL_SEARCH_RADIUS_KM=5
MAX_SEARCH_RADIUS_KM=20
RADIUS_INCREMENT_KM=5 